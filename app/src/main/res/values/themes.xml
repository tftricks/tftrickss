<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash theme shown while the app process starts: yellow crowned mark
         on pure black, matching the in-app branding. -->
    <style name="Theme.TFTricks" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="windowSplashScreenAnimationDuration">400</item>
        <item name="postSplashScreenTheme">@style/Theme.TFTricks.Main</item>
    </style>

    <!-- Main theme: everything visible is drawn by Compose on a black window. -->
    <style name="Theme.TFTricks.Main" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>
    </style>
</resources>
